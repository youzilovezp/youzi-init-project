# 🍈 youzi-init-project · 使用手册

> 对 AI 工具说一句话，就能生成一个完整的管理系统。
>
> 安装请先看 [📦 安装说明.md](安装说明.md)。

---

## 目录

| § | 章节 | 什么时候看 |
|---|---|---|
| 1 | [生成第一个项目](#1生成第一个项目) | 刚装好，第一次用 |
| 2 | [启动项目](#2启动项目) | 项目生成了，要跑起来 |
| 3 | [三种模式](#3三种模式) | 不确定该用哪条命令 |
| 4 | [日常命令速查](#4日常命令速查) | 忘了某个命令 |
| 5 | [加新功能](#5加新功能) | 想加业务模块 |
| 6 | [常见问题](#6常见问题) | 出错了 |

---

## 1. 生成第一个项目

### 1.1 打开 AI 工具

打开 Claude Code / opencode / Codex / EasyCode 中任意一个。

### 1.2 输入命令

在对话框输入（`my-app` 换成你的项目名）：

```
/yz-init-admin my-app
```

### 1.3 回答两个问题

AI 会依次问你：

1. **项目显示名**——浏览器标签页上显示的名字。直接回车 = 用默认值
2. **管理员密码**——登录后台的初始密码。直接回车 = `admin`（本地开发够用）

都可以直接按回车跳过。

### 1.4 等待生成

AI 自动生成完整项目，最后会打印 `✅ 项目生成完成`。

你的电脑上多了一个 `my-app/` 文件夹，包含：

```
my-app/
├── backend/     # 后端（接口 + 数据库）
├── frontend/    # 前端（页面）
├── Makefile     # 命令清单
└── ...
```

---

## 2. 启动项目

### 2.1 进入项目目录

新开一个「终端」窗口，输入：

```bash
cd my-app
```

### 2.2 一键启动

```bash
make dev
```

第一次会自动装依赖、准备数据库，需要几分钟，耐心等。

### 2.3 打开页面

浏览器访问：

| 服务 | 地址 |
|---|---|
| 🖥️ **管理页面** | http://localhost:3000 |
| 📖 接口文档 | http://localhost:8000/docs |

### 2.4 登录

- 账号：`admin`
- 密码：你 1.3 步设置的（没设就是 `admin`）

### 2.5 停止

在终端按 `Ctrl + C`，前后端一起停。

> 💡 想分开看输出？用 `make backend-dev`（终端 A）+ `make frontend-dev`（终端 B）代替 `make dev`。

---

## 3. 三种模式

| 命令 | 生成什么 | 适合 |
|---|---|---|
| `/yz-init-admin my-app` | 前端 + 后端 + 数据库 | 从零做完整管理系统（**推荐**） |
| `/yz-init-server my-api` | 后端 + 数据库 | 已有页面，只缺接口 |
| `/yz-init-ui my-web` | 纯前端（内置模拟数据） | 先看界面长相，不装任何数据库 |

三种模式用法完全一致：AI 问什么答什么，然后按提示启动。

---

## 4. 日常命令速查

在**生成的项目目录**里执行：

### 4.1 启动 / 停止

| 命令 | 作用 |
|---|---|
| `make dev` | 一键启动（首次自动装依赖） |
| `make stop` | 停掉 Docker 中间件 |

### 4.2 账号

| 命令 | 作用 |
|---|---|
| `make reset-admin` | 忘了密码，重置为 `admin` |
| `make admin-pass NEW=新密码` | 改成指定密码 |

### 4.3 数据库

| 命令 | 作用 |
|---|---|
| `make backup` | 备份（存到 `backups/`，带时间戳） |
| `make restore FILE=backups/xxx.sql` | 从备份恢复 |
| `make use-sqlite` | 切到 SQLite（一个文件 = 全部数据，零依赖） |
| `make use-pg` | 切回 PostgreSQL |
| `make db-migrate MSG="描述"` | 改了表结构后生成迁移 |
| `make db-upgrade` | 应用迁移 |

### 4.4 测试

| 命令 | 作用 |
|---|---|
| `make test` | 跑全部测试（不碰开发数据） |

---

## 5. 加新功能

想加一个「订单管理」？不用写代码，让脚本生成，再登记 5 处。

### 5.1 生成模块

在项目目录执行：

```bash
python3 backend/scripts/add_module.py order --title "订单管理"
```

带字段一起生成（推荐）：

```bash
python3 backend/scripts/add_module.py product \
    --title "商品管理" \
    --fields "name:str,price:float:0,stock:int:0"
```

字段写法：`名字:类型:默认值`。类型有 6 种：

| 类型 | 含义 | 页面上的控件 |
|---|---|---|
| `str` | 短文本 | 输入框 |
| `text` | 长文本 | 多行文本框 |
| `int` | 整数 | 数字输入框 |
| `float` | 小数 | 数字输入框 |
| `bool` | 开 / 关 | 开关 |
| `datetime` | 日期时间 | 日期选择器 |

> ⚠️ 字段名只能用英文小写 + 下划线（如 `order_no`），不能用 `id` / `class` 这类保留名。

### 5.2 登记第 1 处：后端模型

打开 `backend/app/models/__init__.py`，加一行：

```python
from app.models.order import Order
```

### 5.3 登记第 2 处：后端路由

打开 `backend/app/api/v1/router.py`，加两行：

```python
from app.api.v1.endpoints.order import router as order_router
api_router.include_router(order_router, prefix='/order', tags=['订单管理'])
```

### 5.4 登记第 3 处：前端路由

打开 `frontend/src/router/index.ts`，在 `children` 里加：

```typescript
{ path: 'order', name: 'Order', component: () => import('@/views/order/index.vue'), meta: { title: '订单管理' } }
```

### 5.5 登记第 4 处：左侧菜单

打开 `frontend/src/layouts/BasicLayout.vue`：

1. 文件顶部 `@vicons/ionicons5` 的 import 里加一个图标，如 `DocumentTextOutline`
2. `const menus = [` 数组里加一行：

```typescript
{ label: '订单管理', key: '/order', icon: renderIcon(DocumentTextOutline) },
```

### 5.6 登记第 5 处：数据库迁移

```bash
make db-migrate MSG="add order"
make db-upgrade
```

### 5.7 刷新页面

重新登录或刷新，左侧菜单出现「订单管理」→ 完成。

---

## 6. 常见问题

### 6.1 输 `/yz-init-admin` 没反应？

AI 工具没重启。完全退出再打开；还不行跑 `./install.sh status` 检查安装。

### 6.2 登录报「服务器内部错误」？

九成是数据库账号不对。看**后端终端**的日志，按里面的中文提示操作：
- 方案 A：在本机 PostgreSQL 里创建日志打印的用户和库
- 方案 B：`backend/.env` 把 `POSTGRES_PORT` 改成 `15432`，让 Docker 起独立实例

### 6.3 端口被占？

```bash
lsof -i :3000    # 前端
lsof -i :8000    # 后端
```

查到进程号后 `kill -9 <进程号>`；或者改 `backend/.env` 里的 `PORT` / `FRONTEND_PORT`。

### 6.4 想改页面标题？

改 `frontend/.env.development` 和 `.env.production` 里的 `VITE_APP_TITLE`，重启前端。

### 6.5 想删掉项目重来？

```bash
cd ..
rm -rf my-app
```

（用了 PostgreSQL 先在项目里 `make stop`。）

### 6.6 默认密码 admin 安全吗？

本地开发没问题。**上线前必须改**：

```bash
cd my-app && make admin-pass NEW='一个强密码'
```

后端在生产模式（`APP_ENV=prod`）会拒绝弱密码启动。

### 6.7 加模块后菜单没出现？

按 5.2 → 5.5 逐处检查，漏一处菜单就不显示。最常见是漏了 5.5 的菜单项。

### 6.8 依赖装得慢？

换国内镜像：

```bash
pnpm config set registry https://registry.npmmirror.com
pip3 config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
```

### 6.9 前端技术栈是什么？

Vue 3 + TypeScript + **Naive UI** 全套生态（组件 / 主题 / `@vicons` 图标），配 Tailwind 原子类做布局。
给 AI 拓展页面时无需额外约束——项目内 AGENTS.md 已写死「只用 Naive UI」。

### 6.10 生产部署怎么做？

核心四步（在项目目录）：

1. `backend/.env` 改成生产数据库参数，`APP_ENV=prod`
2. `SECRET_KEY` 换新的：`openssl rand -hex 32` 填进 `.env`
3. 启动后端：`cd backend && gunicorn app.main:app -w 4 -k uvicorn.workers.UvicornWorker -b 0.0.0.0:8000`
4. 打包前端：`cd ../frontend && pnpm build`，把 `dist/` 交给 Nginx

---

## 术语速查

| 术语 | 通俗解释 |
|---|---|
| Skill | AI 工具的「插件」，教它一套新本事 |
| 终端 | 输命令的那个黑窗口（Mac: Terminal） |
| SQLite | 一个文件就是一个数据库，零安装 |
| 备份 | `make backup`，把数据存一份防丢 |
| 迁移 | 数据库表结构的「改动记录」，可回滚 |

---

## 寻求帮助

1. 复制**完整报错信息**
2. 发给 AI 工具，问：「这个报错怎么解决？」
3. 查本文第 6 节
4. 去 GitHub 提 Issue
