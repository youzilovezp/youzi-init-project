# 🍊 youzi-init-project · 使用手册

> **唯一用户文档** — 入门 / 命令 / 三模式 / 加业务模块 / FAQ / 排错都在这里。
>
> 安装卸载看 [📦 安装说明.md](安装说明.md)；项目总览看 [👋 README.md](README.md)。

## 📑 目录

| § | 章节 | 你适合看吗 | ⏱️ 时间 |
|---|---|---|---|
| § 1 | [三分钟路径（老用户）](#1-三分钟路径老用户) | 用过类似脚手架 | 3 分钟 |
| § 2 | [从零开始（新手）](#2-从零开始新手) | 第一次用 | 10 分钟 |
| § 3 | [速查表](#3-速查表) | 查命令 | 2 分钟 |
| § 4 | [三模式详解](#4-三模式详解) | 想深入 | 5 分钟 |
| § 5 | [加业务模块](#5-加业务模块-add_modulepy) | 要加功能 | 10 分钟 |
| § 6 | [FAQ + 故障排查](#6-faq--故障排查) | 出错了 | 5 分钟 |

---

# § 1 三分钟路径（老用户）

> 📌 **已装好脚手架？直接看下面四步跳过其他全部。**

## 1.1 装 Skill

```bash
cd /path/to/youzi-init-project
./install.sh install
```

> 详见 [📦 安装说明.md](安装说明.md)。装完**完全退出再重开 Claude Code**（Cmd+Q / Alt+F4）。

## 1.2 跑

在 Claude Code 里输入：

```
/yz-init-admin my-app
```

按提示回答（可全回车跳过）→ `my-app/` 目录生成。

## 1.3 启动

```bash
cd my-app
make dev    # 一键启动：装依赖 + 准备中间件 + 后端 + 前端（Ctrl+C 一起停）
```

也可以分开跑（输出更清晰）：

```bash
make install        # 首次：装依赖
make backend-dev    # 终端 A：启动后端（自动准备中间件 + 建表 + 种子账号）
make frontend-dev   # 终端 B：启动前端
```

> 💡 默认 PostgreSQL——`make start`（自动调用）**优先复用本机已运行的**，缺的才用 Docker 起。

## 1.4 访问

| 服务 | 地址 | 默认账号 |
|---|---|---|
| 🖖️ **前端** | http://localhost:3000 | `admin` / `admin` |
| 🔌 后端 API | http://localhost:8000 | — |
| 📖 API 文档 | http://localhost:8000/docs | — |
| 🗄️ 数据库 UI | http://localhost:8080 | adminer（需手动启动：`docker compose --env-file backend/.env up -d adminer`；登录信息见 `backend/.env`） |

> 💡 备份跑 `make backup`（自动存到 `backups/` 并带时间戳）。

## 1.5 加业务模块

```bash
cd my-app
python3 backend/scripts/add_module.py order --title "订单管理"
# 脚本生成 6 文件，提示**5 处手动注册**，按提示改即可

make db-migrate MSG="add order"
make db-upgrade
```

详细：[§ 5](#5-加业务模块-add_modulepy)。

---

# § 2 从零开始（新手）

> 🆕 **第一次用？跟着下面 4 步走，全程约 10 分钟。**

## 2.1 准备工具（一次性）

| 工具 | 怎么检查 | 没装怎么办 |
|---|---|---|
| 🐍 Python 3.11+ | `python3 --version` | [python.org](https://python.org) |
| 🟢 Node.js（LTS） | `node --version` | [nodejs.org](https://nodejs.org) |
| 📦 pnpm | `pnpm --version` | `npm install -g pnpm` |
| 🤖 Claude Code | `claude --version` | [claude.com/download](https://claude.com/download) |
| 🐳 Docker | `docker --version` | [docker.com](https://docker.com)（本机已装 PostgreSQL 可不装） |

> 📌 生成的项目默认 PostgreSQL：**本机已有 PG 直接复用**，没有才用 Docker 起。网络能打开 github.com 即可，公司内网找 IT 代理。

## 2.2 装 Skill

```bash
git clone https://github.com/your-org/youzi-init-project.git
cd youzi-init-project
./install.sh install
```

> 💡 装完会创建 `~/.claude/skills/yz-init-{admin,server,ui}/` 三个目录，然后**完全退出重开 Claude Code**。

## 2.3 第一次跑

打开 Claude Code，输入：

```
/yz-init-admin my-app
```

**示例**：

```
/yz-init-admin my-crm        # CRM 系统
/yz-init-admin my-shop       # 商城后台
/yz-init-admin my-cms        # 内容管理
```

Claude 会问项目显示名 + 初始管理员密码（都可回车跳过）。完成后：

```bash
cd my-app
make dev    # 一键启动（Ctrl+C 一起停）
```

打开 http://localhost:3000，用 `admin` / `admin` 登录。

## 2.4 看懂生成的项目

```
my-app/
├── backend/                 # 后端 FastAPI
│   ├── app/
│   │   ├── models/         # ORM（User / Role）
│   │   ├── schemas/        # Pydantic 协议
│   │   ├── crud/           # 数据访问
│   │   ├── api/v1/         # 接口路由
│   │   └── core/           # 配置 / 安全 / 异常
│   ├── alembic/            # 数据库迁移
│   ├── tests/              # pytest
│   └── docs/               # 架构 / 配置 / 开发 / API 文档
├── frontend/                # 前端 Vue 3
│   ├── src/
│   │   ├── api/ / views/ / router/ / layouts/ / stores/
│   └── docs/
├── docker-compose.yml       # PostgreSQL / Redis / adminer（缺中间件时 Docker 起）
├── Makefile
└── README.md
```

默认带 **User / Role** 两个示例表 + admin 账号，另有 3 个演示账号：`manager` / `alice` / `bob`（密码都是 `Demo@123`，仅本地开发；生产 `AUTO_SEED_DATA=false` 不会创建）。生产前**必须改密码**（详见 § 4.4）。

---

# § 3 速查表

> 🔍 **忘了某个命令？来这里查。**

## 3.1 初始化新项目（在 Claude Code 里）

```
/yz-init-admin my-app    # 完整前后端
/yz-init-server my-api   # 只要后端
/yz-init-ui my-web       # 只要前端
```

## 3.2 Make 命令（在生成项目目录里）

| 命令 | 作用 | 何时用 |
|---|---|---|
| `make dev` | 一键启动：装依赖 + 中间件 + 后端 + 前端 | 日常开发 |
| `make install` | 装后端 + 前端依赖 | 首次（dev 会自动跑） |
| `make use-sqlite` / `make use-pg` | 一键切换数据库模式 | 不想用 PG / 想换回来 |
| `make backend-dev` | 启动后端（自动准备中间件 + 建表 + 种子） | 终端 A |
| `make frontend-dev` | 启动前端（vite 热重载） | 终端 B |
| `make db-migrate MSG="add order"` | 生成迁移文件 | 加新 model 后 |
| `make db-upgrade` | 应用所有迁移 | db-migrate 后 |
| `make db-downgrade` | ⚠️ 回滚一步（需确认） | 出错时 |
| `make reset-admin` | 重置 admin 密码（默认重置为 `admin`） | 忘了密码 |
| `make admin-pass NEW=xxx` | 重置为指定密码 | 改密 |
| `make test` | 跑测试（独立临时测试库，不碰开发数据、免中间件） | 提交前 |
| `make backup` | 备份数据库到 `backups/` | 重要操作前 / 定期 |
| `make restore FILE=backups/app_xxx.sql` | 从备份恢复 | 数据损坏时 |
| `make start` / `make stop` | 中间件启停（**优先复用本机已运行的 PG/Redis**，缺的才起 Docker） | backend-dev 自动调用；单独换中间件时用 |

## 3.3 加业务模块

```bash
# 简单
python3 backend/scripts/add_module.py order --title "订单管理"

# 自定义字段（生成 model 不用手写）
python3 backend/scripts/add_module.py product \
    --title "商品管理" \
    --fields "name:str,price:float:0,stock:int:0,status:str:active"
```

## 3.4 文件位置

| 你想看什么 | 路径 |
|---|---|
| 数据库 | PostgreSQL（默认）/ `backend/data/app.db`（sqlite 模式） |
| 后端 ORM 模型 | `backend/app/models/{<m>}.py` |
| 后端接口 | `backend/app/api/v1/endpoints/{m}.py` |
| 前端页面 | `frontend/src/views/{m}/index.vue` |
| 前端 API | `frontend/src/api/{m}.ts` |
| 数据库迁移 | `backend/alembic/versions/` |
| 后端配置 | `backend/.env` |
| 前端配置 | `frontend/.env.example` |
| API 文档 | http://localhost:8000/docs |

## 3.5 更新 / 卸载

```bash
# 更新脚手架
cd ~/youzi-init-project && git pull
# 重启 Claude Code 让新 Skill 生效

# 卸载 skill
cd ~/youzi-init-project && ./install.sh uninstall

# 删生成项目
rm -rf ~/my-app
```

> 用了 PostgreSQL 模式的项目，删之前先 `make stop` 停掉容器。

---

# § 4 三模式详解

> 🔍 **三种模式有啥区别？啥时候用哪个？**

## 4.1 三种模式对比

| 维度 | admin | server | ui |
|---|---|---|---|
| 后端 | ✅ | ✅ | ❌ |
| 前端 | ✅ | ❌ | ✅ |
| 数据库 | ✅ PostgreSQL（可切 SQLite） | ✅ PostgreSQL（可切 SQLite） | — |
| 默认端口 | 8000 / 3000 | 8000 | 3000 |
| 默认账号 | `admin` / `admin` | `admin` / `admin` | — |
| 典型场景 | 新建完整管理系统 | 已有前端，要后端 | 接已有后端 |

## 4.2 配置 `.env`

**后端** `backend/.env`（init.py 自动生成随机密钥，**不要手改**）：

| 变量 | 说明 | 默认值 |
|---|---|---|
| `DB_TYPE` | 数据库类型：`postgresql`（默认）/ `sqlite`（单文件零配置） | `postgresql` |
| `SQLITE_PATH` | SQLite 文件位置 | `data/app.db` |
| `SECRET_KEY` | JWT 签名密钥（≥ 32 字符） | 自动生成 64 hex |
| `DEBUG` | 调试模式（生产必须 false） | `false` |
| `POSTGRES_*` | PostgreSQL 连接（仅 `DB_TYPE=postgresql` 时生效） | — |
| `REDIS_HOST` | 留空 = 内存模式（默认）；填了启用 Redis（生产多 worker 用） | 空 |
| `CORS_ORIGINS` | 允许的跨域来源 | `localhost:3000` |
| `JWT_EXPIRE_MINUTES` | Token 过期（分钟） | `60` |

**前端** `frontend/.env.development` 和 `.env.production`：

| 变量 | 说明 |
|---|---|
| `VITE_API_BASE_URL` | 后端 API 地址（默认 `/api/v1`） |
| `VITE_APP_TITLE` | 页面标题 |
| `VITE_PROXY_TARGET` | dev proxy 目标（默认 `http://localhost:8000`） |
| `VITE_TOKEN_KEY` | localStorage token key（默认 `access_token`） |

## 4.3 改模板

直接编辑 `templates/backend/app/main.py.tmpl` 等文件。符号链接安装下**自动生效**，不需要 reinstall。

```bash
# 例：给所有项目加一个统一响应包装
cd ~/youzi-init-project
echo '{"code": 0, "message": "ok", "data": null}' >> templates/backend/app/main.py.tmpl
# 下次生成的项目都会带这一行
```

## 4.4 生产部署

```bash
cd my-app

# 1. backend/.env 切生产数据库
#    DB_TYPE=postgresql
#    POSTGRES_HOST / POSTGRES_PASSWORD / POSTGRES_DB 按实际填
make start            # 中间件就绪（复用本机已有的，缺的用 Docker 补）
make db-upgrade       # 应用迁移

# 2. 生成全新强密钥并替换
SECRET_KEY=$(openssl rand -hex 32)
sed -i '' "s|^SECRET_KEY=.*|SECRET_KEY=${SECRET_KEY}|" backend/.env    # macOS
# sed -i "s|^SECRET_KEY=.*|SECRET_KEY=${SECRET_KEY}|" backend/.env    # Linux

# 3. 启动后端（gunicorn 已在依赖里）
cd backend && gunicorn app.main:app -w 4 -k uvicorn.workers.UvicornWorker -b 0.0.0.0:8000

# 4. 前端打包（产物在 frontend/dist/，丢给 Nginx / 静态托管）
cd ../frontend && pnpm build
```

**整套 Docker 化部署**（后端 + 数据库一起）：

```bash
cd my-app
docker compose --env-file backend/.env up -d --build
```

**安全清单**：

- [ ] 修改默认 admin 密码（用 `--admin-pass` 重新 init）
- [ ] `SECRET_KEY` 用 `openssl rand -hex 32` 生成
- [ ] `DEBUG=false` / `DB_ECHO=false`
- [ ] `CORS_ORIGINS` 只保留可信域名
- [ ] HTTPS（Let's Encrypt / certbot）
- [ ] 数据库强密码 + 仅内网访问
- [ ] 生产关闭 adminer 或加认证
- [ ] 日志聚合（Sentry / ELK）

---

# § 5 加业务模块（`add_module.py`）

> 🧩 **加新功能最方便的方式**：一个命令生成 6 个文件，不用手写 4-5 处样板。

## 5.1 命令

```bash
python3 backend/scripts/add_module.py <name> [选项]
```

| 参数 / 选项 | 默认值 | 说明 |
|---|---|---|
| `name` | 必填 | 模块名，snake_case，如 `order` / `product` |
| `--title` | 由 name 推 | 中文标题（菜单 / 路由 / 接口 summary） |
| `--fields` | `name:str` | 自定义字段，见 [§ 5.2](#52-字段语法) |
| `--backend-dir` | `backend` | 后端目录 |
| `--frontend-dir` | `frontend` | 前端目录 |
| `--force` | 关 | 强制覆盖已存在文件（默认拒绝，保护你的手改） |

## 5.2 字段语法

```
name:type[:default]
```

| 段 | 必填 | 说明 |
|---|---|---|
| name | ✅ | 小写字母 / 下划线开头，仅字母数字下划线 |
| type | ✅ | `str` / `text` / `int` / `float` / `bool` / `datetime` |
| def | ❌ | 默认值；省略 = 必填（nullable=False） |

### 5.2.1 类型映射

| 类型 | SQLAlchemy | Pydantic | TS | 表单控件 |
|---|---|---|---|---|
| `str` | `String(100)` | `str` | `string` | `el-input` |
| `text` | `Text` | `str` | `string` | `el-input textarea` |
| `int` | `Integer` | `int` | `number` | `el-input-number` |
| `float` | `Numeric(10, 2)` | `float` | `number` | `el-input-number` |
| `bool` | `Boolean` | `bool` | `boolean` | `el-switch` |
| `datetime` | `DateTime` | `datetime` | `string` | `el-date-picker` |

### 5.2.2 默认值写法

| 场景 | 写法 | 解析后 |
|---|---|---|
| 必填（无默认） | `name:str` | `nullable=False` |
| str 默认 | `status:str:active` | `default='active'` |
| int 默认 | `stock:int:0` | `default=0` |
| float 默认 | `price:float:9.99` | `default=9.99` |
| bool 默认 | `enabled:bool:true` | `default=True` |

str 默认**不需要写引号**；写了也行（`status:str:'active'` ✅）。

### 5.2.3 禁止的字段名

- 保留名：`id` / `created_at` / `updated_at`（由 TimestampMixin 自动生成）
- Python / JS 关键字：`class` / `type` / `import` / `return` 等
- 非法字符：含 `;` `"` `'` ` ` 等任何非 `[a-z0-9_]` 字符

## 5.3 完整示例：商品模块

```bash
python3 backend/scripts/add_module.py product \
    --title "商品管理" \
    --fields "name:str,price:float:0,stock:int:0,status:str:active,desc:text"
```

生成 6 个文件：

**`backend/app/models/product.py`**：
```python
"""商品管理 ORM 模型。"""
from sqlalchemy import Integer, Numeric, String, Text
from sqlalchemy.orm import Mapped, mapped_column

from app.models.base_class import Base, TimestampMixin


class Product(Base, TimestampMixin):
    __tablename__ = "products"

    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    name: Mapped[str] = mapped_column(String(100), nullable=False)
    price: Mapped[float] = mapped_column(Numeric(10, 2), default=0.0)
    stock: Mapped[int] = mapped_column(Integer, default=0)
    status: Mapped[str] = mapped_column(String(100), default='active')
    desc: Mapped[str] = mapped_column(Text, nullable=False)
```

**`backend/app/schemas/product.py`**：`ProductBase` / `Create` / `Update` / `Out` 4 个 Pydantic 模型。

**`backend/app/crud/product.py`**：`product_crud = CRUDBase[Product, ProductCreate, ProductUpdate](Product)` 一行。

**`backend/app/api/v1/endpoints/product.py`**：5 个标准接口（列表 / 创建 / 详情 / 更新 / 删除），全部要求 SuperUser。

**`frontend/src/api/product.ts`**：3 个 TS interface + 5 个函数。

**`frontend/src/views/product/index.vue`**：Element Plus 单页（分页列表 + 新增/编辑弹窗 + 删除确认）。

## 5.4 跑完后手动注册 5 处

脚本只生成新文件，**故意不改旧文件**（避免误改你的代码）：

1. `backend/app/models/__init__.py` 加：
   ```python
   from app.models.product import Product
   ```

2. `backend/app/api/v1/router.py` 加：
   ```python
   from app.api.v1.endpoints.product import router as product_router
   api_router.include_router(product_router, prefix='/product', tags=['商品管理'])
   ```

3. `frontend/src/router/index.ts` 加路由：
   ```typescript
   { path: 'product', name: 'Product', component: () => import('@/views/product/index.vue'), meta: { title: '商品管理', icon: 'Goods' } }
   ```

4. `frontend/src/layouts/BasicLayout.vue` 的 `const menus = [` 数组里加一行（菜单是数据驱动渲染的）：
   ```ts
   { path: '/product', title: '商品管理', icon: 'Goods' },
   ```
   > icon 填 Element Plus 图标名的字符串（如 `Goods` / `Document` / `Setting`），已全局注册无需 import。

5. 数据库迁移：
   ```bash
   make db-migrate MSG="add product"
   make db-upgrade
   ```

## 5.5 命名规范

| 场景 | 规则 | 例子 |
|---|---|---|
| 模块名 | snake_case，小写字母开头 | `order` ✅ / `Order` ❌ / `订单` ❌ |
| 字段名 | snake_case，非保留名 | `price` ✅ / `id` ❌ / `class` ❌ |
| 标题 | 中文，会显示在菜单 / 路由 / 接口 summary | `订单管理` ✅ |

## 5.6 实际场景示例

```bash
# 用户资料
python3 backend/scripts/add_module.py user_profile \
    --title "用户资料" \
    --fields "nickname:str,email:str,phone:str,avatar:str,bio:text"

# 文章（带开关 + 计数）
python3 backend/scripts/add_module.py article \
    --title "文章管理" \
    --fields "title:str,content:text,views:int:0,published:bool:false,sort:int:0"

# 订单明细（全必填）
python3 backend/scripts/add_module.py order_item \
    --title "订单明细" \
    --fields "order_id:int,product_id:int,quantity:int,price:float"
```

## 5.7 `--fields` 不够用？手动加

外键 / 索引 / 关系 / 唯一约束 / enum，`--fields` 不支持，直接编辑生成的文件：

```python
# 在 model 里加索引
name: Mapped[str] = mapped_column(String(100), nullable=False, index=True)

# 加外键
user_id: Mapped[int] = mapped_column(ForeignKey("users.id"), nullable=False)

# 加关系
items: Mapped[list["OrderItem"]] = relationship(back_populates="order")
```

加完跑迁移：
```bash
make db-migrate MSG="add fk + index"
make db-upgrade
```

## 5.8 二次跑覆盖

默认拒绝覆盖已存在文件，防止误删你手动改的代码。确认要覆盖就加 `--force`：

```bash
python3 backend/scripts/add_module.py product --title "商品管理" --force
```

---

# § 6 FAQ + 故障排查

## 6.1 FAQ

### Q：三个 skill 有什么区别？
看 [§ 4.1 三种模式对比](#41-三种模式对比)。

### Q：备份怎么恢复？
```bash
make restore FILE=backups/app_20260825_220000.sql   # PG 备份
make restore FILE=backups/app_20260825_220000.db    # SQLite 备份
```

### Q：我的数据存在哪？
默认在 PostgreSQL（Docker 卷或本机实例）。**备份跑 `make backup`**（自动存到 `backups/` 带时间戳）。想要"一个文件就是全部数据"：`.env` 改 `DB_TYPE=sqlite`。

### Q：项目名起错了能改吗？
**不建议直接改 .env**——重命名要改 8 处地方。建议删了重跑：
```bash
rm -rf my-app
python3 /path/to/youzi-init-project/scripts/init.py new-name --only admin
```

显示名（页面标题）随时可改：
```bash
# macOS（BSD sed）：
sed -i '' 's/VITE_APP_TITLE=.*/VITE_APP_TITLE=我的商城/' my-app/frontend/.env.development
sed -i '' 's/VITE_APP_TITLE=.*/VITE_APP_TITLE=我的商城/' my-app/frontend/.env.production
# Linux / WSL（GNU sed，去掉 ''）：
# sed -i 's/VITE_APP_TITLE=.*/VITE_APP_TITLE=我的商城/' my-app/frontend/.env.development
# 或直接用编辑器修改文件
```

### Q：多个项目同时开发，端口冲突怎么办？
改 `backend/.env`：`PORT=8001`（后端）、`FRONTEND_PORT=3001`（前端）、`POSTGRES_PORT=15433`（避免共用一套 PG）。`make dev` / `make start` 都会读取这些值。前端还要把 `frontend/.env.development` 的 `VITE_PROXY_TARGET` 指向新后端端口。

### Q：能改默认带的功能吗？
能。改 `templates/backend/app/models/user.py` 等文件，符号链接模式下自动生效。

### Q：生成的代码能直接上线吗？
骨架可以，业务一定要改。生产清单看 [§ 4.4](#44-生产部署)。

### Q：忘了 admin 密码？
```bash
cd my-app && make reset-admin     # 重置为 admin
# 或指定：
cd my-app && make admin-pass NEW=YourStrongPass2025
```

### Q：加新业务模块后，接口 404？
按 [§ 5.4](#54-跑完后手动注册-5-处) 重新检查 5 处注册，最常见是漏了 `include_router(...)`。

### Q：升级到新版本脚手架会破坏已有项目吗？
不会。`git pull` 只更新脚手架本身，**已生成项目不受影响**。

### Q：`--fields` 里能加外键吗？
不能。手动修复。详见 [§ 5.7](#57--fields-不够用手动加)。

### Q：字段名能是中文吗？
不能。字段名是 Python 标识符。

### Q：多对多关系怎么生成？
`--fields` 不支持。先跑两个单表，再在 model 里手动加 `relationship(...)`。

### Q：`/yz-init-*` 跟 `python3 scripts/init.py <name>` 等价吗？
等价，只是入口不同：
- `scripts/init.py` 是 CLI，适合脚本 / debug
- `/yz-init-*` 是 Claude 入口，自动问显示名 / 密码

### Q：默认密码 admin/admin 安全吗？
**仅供本地开发**。已有项目改密（推荐）：
```bash
cd my-app && make admin-pass NEW='<强密码>'
```
还没生成的新项目，生成时就指定：
```bash
python3 scripts/init.py my-app --admin-pass '<强密码>' --only admin
# 注意：init.py 不能对已存在的目录重跑（会报"目标目录已存在"）
# 后端在 APP_ENV=prod 时会自动检查，弱密码拒绝启动
```

## 6.2 故障排查

### 6.2.1 高频排错

| 问题 | 解决 |
|---|---|
| `/yz-init-admin: command not found` | 重启 Claude Code；`ls ~/.claude/skills/yz-init-admin/SKILL.md` |
| `ModuleNotFoundError: jinja2` | `pip3 install jinja2` |
| `pnpm: command not found` | `npm install -g pnpm` |
| `docker: command not found` | 本机也没有 PostgreSQL 时装 [Docker](https://docker.com)；或 `.env` 改 `DB_TYPE=sqlite` 免中间件 |
| 后端启动报 `SECRET_KEY 少于 32 字符` | `.env` 是从 `.env.example` 复制的。重新跑 init.py 生成，或手动 `openssl rand -hex 32` 替换 |
| 前端菜单不显示新增模块 | 检查 `frontend/src/layouts/BasicLayout.vue` 的 `menus` 数组是否加了菜单项 |
| 后端起来了但**登录返回 500**（页面只显示"服务器内部错误 trace_id=…"） | 九成是数据库认证失败：本机 5432 已有 PostgreSQL → 项目复用了它，但 `.env` 里的用户/密码/库在该实例不存在。**看后端终端日志**（有完整中文解法）。两个出路：① 在本机 PG 里执行日志打印的 `create user / create database`；② 改 `.env` 的 `POSTGRES_PORT` 为空闲端口（如 15432）→ `make start` 用 Docker 起独立实例 |

### 6.2.2 命令不识别

```bash
ls ~/.claude/skills/yz-init-admin/SKILL.md   # 必须存在
# 不存在 → 重装：
cd ~/youzi-init-project && ./install.sh install
# 然后**完全退出** Claude Code（Cmd+Q）再重开
```

### 6.2.3 端口被占

```bash
lsof -i :3000     # 前端
lsof -i :8000     # 后端
kill -9 <PID>
# PG 模式另有：5432（PostgreSQL）/ 6379（Redis）/ 8080（adminer）
```

### 6.2.4 后端启动失败

```bash
cd my-app/backend
cat .env | head -20            # 检查配置
# 重装依赖（优先用 make install——它会建 venv；手动方式如下）
source .venv/bin/activate      # 没有就 python3 -m venv .venv 先建
pip install -e ".[dev]"        # 或 uv pip install -e ".[dev]"
```

### 6.2.5 `make db-migrate` 失败 / 报"No changes detected"

```bash
grep "from app.models.order" my-app/backend/app/models/__init__.py
# 必须有这一行，alembic 才能看到 model
```

**首次启动后**必须手动建迁移基线（这是设计，避免循环依赖）：

```bash
make db-migrate MSG="init"
make db-upgrade
```

### 6.2.6 `pnpm install` 慢 / 失败

```bash
npm config set registry https://registry.npmmirror.com
pnpm config set registry https://registry.npmmirror.com
pnpm install
```

### 6.2.7 add_module 跑完前端菜单没出来

按 [§ 5.4](#54-跑完后手动注册-5-处) 检查 5 处注册：

```bash
grep -rn "from app.models.order" my-app/backend/
grep -rn "order_router" my-app/backend/app/api/v1/
grep -rn "'order'" my-app/frontend/src/router/
grep -rn "订单管理" my-app/frontend/src/layouts/
```

漏哪一步菜单就不显示。

### 6.2.8 VSCode Pylance 报 import 错误

```json
// my-app/backend/.vscode/settings.json
{ "python.analysis.extraPaths": ["./app"] }
```

### Q：`make start` 报 "'compose' is not a docker command"？
Docker Compose v1（老版本）。升级 Docker Desktop / `sudo apt install docker-compose-plugin` 后用 `docker compose`。

## 6.3 术语速查

| 术语 | 通俗解释 |
|---|---|
| **Skill** | Claude Code 的"插件"，扩展 Claude 的能力 |
| **SQLite** | 一个文件就是一个数据库（`DB_TYPE=sqlite` 时用） |
| **Symlink** | 文件夹的"快捷方式"（修改原始 → 链接同步更新） |
| **ORM** | 把数据库表当 Python 类用的工具 |
| **JWT** | 登录后给前端的"通行证"，证明你登录过 |
| **Alembic** | 数据库迁移工具（改表结构不用手动写 SQL） |
| **CRUD** | Create / Read / Update / Delete |
| **adminer** | 网页版数据库 UI，看/改数据用 |

---

# 🎁 进阶玩法

## 批量生成多个项目

```bash
#!/bin/bash
cd ~/youzi-init-project
for m in product category order payment; do
  case "$m" in
    product)  title="商品管理" ;;
    category) title="分类管理" ;;
    order)    title="订单管理" ;;
    payment)  title="支付记录" ;;
  esac
  python3 scripts/init.py "$m" --only admin --title "$title"
done
```

## 集成到 Git

```bash
cd my-app
git init && git add . && git commit -m "init: yz-init-admin"
git remote add origin <your-repo-url>
git push -u origin main
```

---

# 🆘 寻求帮助

1. **看错误信息** — 99% 的错误都有线索
2. **复制整个错误** 发给 Claude Code："这个报错是什么意思？"
3. **查本文档的 § 6.2** — 高频排错
4. **GitHub Issues** — 提 bug 或功能建议

---

**祝你玩得开心！** 🎉
