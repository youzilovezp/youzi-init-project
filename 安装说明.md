# 安装 / 卸载 / 更新 / 使用参考

> [项目说明.md](README.md) 已经覆盖了快速上手。本文档面向需要深入了解安装与运维的用户。

---

## 一、安装

### 1.1 一键安装（推荐）

**如果是刚 clone 下来的仓库**（或解压的 tarball），直接：

```bash
git clone https://github.com/your-org/youzi-init-project.git
cd youzi-init-project
./install.sh install
```

**如果已经在本地**：直接 cd 进项目目录后跑 `./install.sh install`。

脚本会自动完成：

1. 验证源目录（含 `templates/skills/*/SKILL.md`）
2. 创建 `~/.claude/skills/`
3. 逐个安装三个 skill：`yz-init-admin` / `yz-init-server` / `yz-init-ui`
4. 共享 `scripts/` 和 `templates/`（符号链接，修改源仓库即时生效）

### 1.2 高级选项

```bash
./install.sh install --dir ~/my-skills   # 自定义安装目录
./install.sh install --mode copy         # 复制模式（不创建符号链接）
./install.sh help                        # 查看所有选项
```

---

## 二、卸载

```bash
./install.sh uninstall
```

---

## 三、更新

**符号链接模式（默认）**：直接编辑源目录，重启 Claude Code 即可生效。无需 update。

**复制模式**：

```bash
./install.sh update
```

---

## 四、使用方式

### 4.1 通过 Claude Code（推荐）

| 命令                     | 输出                                  |
| ------------------------ | ------------------------------------- |
| `/yz-init-admin <name>`  | 后端 + 前端 + 中间件 + 数据库自动维护 |
| `/yz-init-server <name>` | 后端 + 中间件 + 数据库自动维护        |
| `/yz-init-ui <name>`     | 纯前端                                |

**流程**：在 Claude Code 输入命令 → Claude 询问项目显示名 + 初始管理员密码（都可回车跳过）→ 输出 `项目名/` 目录。

### 4.2 各模式启动指引

| 模式   | 启动（执行顺序）                                                         |
| ------ | ------------------------------------------------------------------------ |
| admin  | `make install` → `make start` → `make backend-dev` + `make frontend-dev` |
| server | `make install` → `make start` → `make backend-dev`                       |
| ui     | `pnpm install` → `pnpm dev`                                              |

### 4.3 启动后访问

- 前端：http://localhost:3000
- 后端 API：http://localhost:8000
- API 文档：http://localhost:8000/docs
- 数据库 UI（admin）：http://localhost:8080

### 4.4 直接用脚本（不通过 Claude Code）

```bash
python3 /path/to/youzi-init-project/scripts/init.py my-admin
python3 /path/to/youzi-init-project/scripts/init.py my-api --only server
python3 /path/to/youzi-init-project/scripts/init.py my-web --only ui
```

---

## 五、常见问题

### Q1: Claude Code 中看不到三个 skill 命令？

检查 `~/.claude/skills/` 下有 `yz-init-admin`、`yz-init-server`、`yz-init-ui` 三个子目录，每个含 `SKILL.md`。缺失则重跑 `./install.sh install`，最后**重启 Claude Code**。

### Q2: 修改了模板但命令没生效？

`SKILL.md` 在 Claude Code 启动时读取一次，需重启 Claude Code。修改 `templates/` 不需要重启（符号链接共享）。

### Q3: 后端启动失败 / .env 没生效？

检查 `.env` 中 `SECRET_KEY` 是否设置（init.py 会自动生成）。位置：

- admin 模式：`backend/.env`
- server 模式：项目根目录 `.env`

### Q4: 忘了 admin 密码怎么办？

```bash
cd my-app
make reset-admin        # 生成新随机密码并打印
# 或指定密码：
make admin-pass NEW=YourNewPass!2025
```

### Q5: 多个项目如何共享 Skill？

符号链接模式天然支持：所有项目共享 `~/.claude/skills/yz-init-*`。修改源仓库对所有生效。

### Q6: 生产环境要注意什么？

1. 修改默认账号 admin / 启动时打印的随机密码（用 `make reset-admin` 或 `make admin-pass`）
2. 用 `openssl rand -hex 32` 生成新的 `SECRET_KEY`
3. 关闭 `DEBUG` 和 `DB_ECHO`
4. `LOG_FORMAT` 改为 `json` 便于日志聚合
5. `CORS_ORIGINS` 只保留可信域名

---

## 六、许可证

MIT
