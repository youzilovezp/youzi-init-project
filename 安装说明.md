# 📦 柚子脚手架 · 安装说明

> **目标**:把 `/yz-init-*` 装到你的 Claude Code,从 0 到能跑通 `/yz-init-admin <name>`。
>
> 本文档**只讲安装 / 卸载 / 更新**。所有用法看 [📘 使用手册.md](使用手册.md)。

---

## 📑 目录

1. [系统要求](#1-系统要求)
2. [一键安装](#2-一键安装)
3. [高级选项](#3-高级选项)
4. [验证安装](#4-验证安装)
5. [更新](#5-更新)
6. [卸载](#6-卸载)
7. [常见安装问题](#7-常见安装问题)

---

## 1. 系统要求

| 工具            | 最低版本                        | 检查命令                     | 没装怎么办                                                          |
| --------------- | ------------------------------- | ---------------------------- | ------------------------------------------------------------------- |
| **Python**      | 3.11+                           | `python3 --version`          | Mac: `brew install python3` · Win: [python.org](https://python.org) |
| **Docker**      | 任意                            | `docker --version`           | [docker.com](https://docker.com)                                    |
| **jinja2**      | 任意                            | `python3 -c "import jinja2"` | `pip install jinja2`                                                |
| **Claude Code** | v1.0+                           | `claude --version`           | [claude.com/download](https://claude.com/download)                  |
| **Git**         | 任意                            | `git --version`              | Mac: `brew install git` · Win: [git-scm.com](https://git-scm.com)   |
| **网络**        | 出口可达 github.com / claude.ai | 浏览器试一下                 | 公司内网找 IT 代理                                                  |

**磁盘占用**:约 30 MB(脚手架源)+ Docker 镜像约 1 GB(Python 3.11 + PostgreSQL 16 + Redis 7)。

---

## 2. 一键安装

### 2.1 方式 A · Git clone(推荐)

```bash
# 1. 下载脚手架
git clone https://github.com/your-org/youzi-init-project.git ~/youzi-init-project
cd ~/youzi-init-project

# 2. 装到 Claude Code
./install.sh install

# 3. 完全退出 Claude Code(Cmd+Q)再重开
```

### 2.2 方式 B · 下载 ZIP

1. 到 GitHub 页面点 `Code` → `Download ZIP`
2. 解压到任意位置(如 `~/Documents/youzi-init-project`)
3. 终端 cd 进去跑 `./install.sh install`

### 2.3 安装过程做什么?

`install.sh install` 自动完成:

1. ✅ 验证源目录(含 `templates/skills/*/SKILL.md`)
2. ✅ 创建 `~/.claude/skills/`
3. ✅ 逐个安装三个 skill:
   - `~/.claude/skills/yz-init-admin/`
   - `~/.claude/skills/yz-init-server/`
   - `~/.claude/skills/yz-init-ui/`
4. ✅ 共享 `scripts/` 和 `templates/`(**符号链接**,改源仓库即时生效)

---

## 3. 高级选项

```bash
# 自定义 skill 安装目录
./install.sh install --dir ~/my-skills

# 完整复制(不创建符号链接)
./install.sh install --mode copy

# 看所有选项
./install.sh help
```

**两种安装模式对比**:

| 模式           | 优点                                        | 缺点                                 |
| -------------- | ------------------------------------------- | ------------------------------------ |
| **link**(默认) | 改源仓库 → 所有项目即时生效(无需 reinstall) | 源目录被删 / 移动 → 软链接失效       |
| **copy**       | 独立副本,源删了也不影响                     | 改源 → 必须 `install.sh update` 同步 |

**推荐**:`link`(开发脚手架的人)/ `copy`(只用不改的人)。

---

## 4. 验证安装

### 4.1 一键验证脚本

```bash
./install.sh status
```

期望输出:

```
== youzi-init-project 安装状态 ==

  源目录：    /Users/you/youzi-init-project
  目标目录：  /Users/you/.claude/skills

  [yz-init-admin]
    状态：    已安装（符号链接）
    scripts：/Users/you/youzi-init-project/scripts
    ✅ 指向当前仓库，修改即时生效
    SKILL.md：✅

  [yz-init-server]    ✅
  [yz-init-ui]        ✅

  环境检查：
    ✅ python3: Python 3.13.2
    ✅ jinja2: 3.1.4
```

### 4.2 跑一次真命令

打开 Claude Code,输入:

```
/yz-init-admin __test-project
```

按提示回答(可全回车跳过),几分钟后 `__test-project/` 目录生成 = **安装成功** ✅

> ⚠️ 测完删掉 `__test-project/`(`rm -rf __test-project`),这是测试项目。

### 4.3 三个 skill 都在吗?

```bash
ls ~/.claude/skills/
# 看到这 3 个目录 = OK:
#   yz-init-admin/   yz-init-server/   yz-init-ui/

ls ~/.claude/skills/yz-init-admin/SKILL.md   # 文件存在 = OK
```

---

## 5. 更新

### 5.1 符号链接模式(默认)

**无需任何操作!**改源仓库后,所有项目**自动反映**新内容(下次 Claude Code 启动时读取 SKILL.md,需重启)。

```bash
cd ~/youzi-init-project
git pull
# 重启 Claude Code(让 SKILL.md 重新加载)
```

### 5.2 复制模式

```bash
cd ~/youzi-init-project
git pull
./install.sh update
# 重启 Claude Code
```

### 5.3 更新脚手架会破坏已生成的项目吗?

**不会!**`install.sh update` 只刷新 `~/.claude/skills/yz-init-*` 软链接,**已生成项目不受影响**。

要更新已生成项目的某个文件,只能:

1. 对比新旧模板
2. 手动迁移到已有项目
3. 或 `git diff` 模板 vs 已生成项目,选择性复制

---

## 6. 卸载

### 6.1 卸载 skill

```bash
cd ~/youzi-init-project
./install.sh uninstall
```

输出:

```
== 卸载三个 Skill ==
  ✅  已删除 yz-init-admin
  ✅  已删除 yz-init-server
  ✅  已删除 yz-init-ui
```

### 6.2 删源(可选)

```bash
rm -rf ~/youzi-init-project
```

### 6.3 删生成项目(可选)

```bash
rm -rf ~/Documents/my-app ~/Documents/my-crm ...
```

### 6.4 清 Docker 中间件(可选)

```bash
# 在每个生成项目目录下:
cd my-app && make stop && docker-compose down --volumes
```

---

## 7. 常见安装问题

### Q1:`python: command not found`

**原因**:Python 没装或没加 PATH。
**解决**:

- Mac: `brew install python3` 或 [python.org](https://python.org) 下载
- Linux: `sudo apt install python3.11`
- 用 `python3` 代替 `python`(macOS 自带 python3)

### Q2:`pip install jinja2` 报 Microsoft C++ Build Tools 缺失

Windows 用户:`pip install jinja2` 通常不需要 C++,只有装 `cryptography` / `lxml` 这种 C 扩展才需要。

### Q3:`docker: command not found`

**解决**:

1. [docker.com](https://docker.com) 下载 Docker Desktop
2. 启动后等顶栏图标变绿
3. 终端验证 `docker ps` 不报错

### Q4:`./install.sh install` 报 "缺少 templates/ 目录"

**原因**:当前目录不是有效的脚手架仓库。
**解决**:

```bash
# 确认当前在仓库根目录
ls templates/skills/admin/SKILL.md   # 必须存在
# 不存在 → 重新 git clone
```

### Q5:`./install.sh install` 报 "目标已存在"

**原因**:`~/.claude/skills/yz-init-admin` 等已存在(可能装过一次)。
**解决**:按提示输入 `y` 替换,或手动:

```bash
rm -rf ~/.claude/skills/yz-init-admin \
       ~/.claude/skills/yz-init-server \
       ~/.claude/skills/yz-init-ui
./install.sh install
```

### Q6:装好了但 Claude Code 不识别 `/yz-init-admin`

**解决**:

1. **完全退出 Claude Code**(Cmd+Q / Alt+F4)
2. 重新打开
3. 输入 `/yz-init-admin`,看是否有自动补全
4. 还不行:删 skill 重装 → `./install.sh install`

### Q7:中文用户跑出乱码或字体问题

```bash
# 终端 locale
export LANG=en_US.UTF-8
export LC_ALL=en_US.UTF-8
echo "export LANG=en_US.UTF-8" >> ~/.zshrc   # 永久生效

# 浏览器字体(Mac/Win 一般自带,Linux):
sudo apt install fonts-noto-cjk
```

### Q8:可以装在 WSL / Linux 服务器吗?

**可以**。脚手架是纯 Python + Docker,跨平台:

```bash
# Ubuntu 22.04+
sudo apt update
sudo apt install -y python3.11 python3-pip
pip3 install jinja2

# Docker
curl -fsSL https://get.docker.com | sh
sudo usermod -aG docker $USER
# 重新登录

# 装 skill
cd ~/youzi-init-project
./install.sh install
```

### Q9:可以多台机器共享同一个脚手架仓库吗?

**可以**。每台机器单独 `./install.sh install`,共享 Git 仓库:

```bash
# 机器 A
git clone https://github.com/your-org/youzi-init-project.git
./install.sh install

# 机器 B
git clone https://github.com/your-org/youzi-init-project.git
./install.sh install
```

---

## ✅ 安装完成的标志

✅ `./install.sh status` 输出 3 个 skill 都 ✅
✅ Claude Code 里输入 `/yz-init-admin` 有自动补全
✅ `/yz-init-admin __test` 跑出 `__test/` 目录

**下一步**:看 [📘 使用手册.md](使用手册.md) §1.4 跑通第一个项目。

---

## 📚 相关文档

| 你想看什么       | 跳到                                          |
| ---------------- | --------------------------------------------- |
| 项目总览         | [README.md](README.md)                        |
| 极速上手(3 分钟) | [📘 使用手册.md](使用手册.md) §1 三分钟路径   |
| 完全新手版       | [📘 使用手册.md](使用手册.md) §2 从零开始     |
| 使用手册 + FAQ   | [📘 使用手册.md](使用手册.md)                 |
| Skill 模板       | `templates/skills/{admin,server,ui}/SKILL.md` |

---

**版本**:v1.0 · 2026-08-24
