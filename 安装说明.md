# 📦 youzi-init-project · 安装说明（Claude Code + opencode 双平台）

> 只讲安装 / 更新 / 卸载，全程约 3 分钟。怎么用看 [📖 使用手册.md](使用手册.md)。

## 1. 先装好这几样

| 工具 | 检查命令 | 没装的怎么办 |
|---|---|---|
| 🐍 Python 3.11+ | `python3 --version` | [python.org](https://python.org) 下载安装 |
| 🟢 Node.js（LTS） | `node --version` | [nodejs.org](https://nodejs.org) 下载安装 |
| 📦 pnpm | `pnpm --version` | `npm install -g pnpm` |
| 🤖 Claude Code **或** opencode | `claude --version` / `opencode --version` | [claude.com/download](https://claude.com/download) · [opencode.ai](https://opencode.ai)（二选一） |
| 🐳 Docker | `docker --version` | [docker.com](https://docker.com)（本机已装 PostgreSQL 的话不需要） |
| 🌿 Git | `git --version` | Mac 自带 · Linux: `sudo apt install git` |

> 💡 生成的项目默认用 **PostgreSQL**：`make start` 会优先**复用本机已运行的**（本机装过 PG 就不需要 Docker），没有才用 Docker 起一套。想要零依赖单文件体验，生成后把 `.env` 改 `DB_TYPE=sqlite` 即可。
>
> ⚠️ **目前支持 macOS / Linux**。Windows 用户请先用 [WSL](https://learn.microsoft.com/windows/wsl/install)（在 PowerShell 里运行 `wsl --install`，重启后按提示初始化，再在里面装上表工具）。

## 2. 安装（就这两步）

```bash
git clone https://github.com/your-org/youzi-init-project.git ~/youzi-init-project
cd ~/youzi-init-project && ./install.sh install
```

自动探测已装的 AI 工具并**同时安装**：

| 工具 | 安装位置 | 触发方式 |
|---|---|---|
| Claude Code | `~/.claude/skills/yz-init-*` | 输入 `/yz-init-admin`（自动补全） |
| opencode | `~/.config/opencode/skills/yz-init-*` + `command/yz-init-*.md` | 同样输入 `/yz-init-admin`（自动补全） |

> 💡 opencode 的 `/` 补全读的是 `command/` 目录——install.sh 会自动生成对应的转发命令，无需手动创建。

只用其中一个？`./install.sh install --platform opencode`（或 `claude`）。

> ⚠️ **完全退出 AI 工具再重开**（Claude Code: Cmd+Q；opencode 重开终端），否则识别不到新技能。

## 3. 验证

```bash
./install.sh status
```

两个平台各 3 个技能都是 ✅ 就装好了。也可以打开 Claude Code / opencode 输入 `/yz-init`，出现自动补全 = 成功。

## 4. 更新

```bash
cd ~/youzi-init-project && git pull
```

重启 AI 工具（Claude Code / opencode）生效。已生成的项目不受影响。

## 5. 卸载

```bash
cd ~/youzi-init-project && ./install.sh uninstall   # 删技能（全部平台）
rm -rf ~/youzi-init-project                         # 删源码（可选）
```

## 6. 常见问题

| 问题 | 解决 |
|---|---|
| `python: command not found` | 用 `python3`（Mac 自带）；仍没有就按上表装 |
| 提示缺 jinja2 | `pip3 install jinja2` |
| `pnpm: command not found` | `npm install -g pnpm` |
| 装完不识别 `/yz-init-admin` | 完全退出 AI 工具再重开（opencode 重开终端）；还不行跑 `./install.sh status` 看技能位置 |
| 提示「目标已存在」 | 之前装过，按提示输 `y` 覆盖即可 |
| 报「缺少 templates/ 目录」 | 确认在仓库根目录执行，或重新 clone |

---

**下一步**：打开 Claude Code 或 opencode 输入 `/yz-init-admin my-app`，跟着 [📖 使用手册.md](使用手册.md) 走。
