# 📦 youzi-init-project · 安装说明

> 只讲安装 / 更新 / 卸载，全程约 3 分钟。怎么用看 [📖 使用手册.md](使用手册.md)。

## 1. 先装好这几样

| 工具 | 检查命令 | 没装的怎么办 |
|---|---|---|
| 🐍 Python 3.11+ | `python3 --version` | [python.org](https://python.org) 下载安装 |
| 🟢 Node.js（LTS） | `node --version` | [nodejs.org](https://nodejs.org) 下载安装 |
| 📦 pnpm | `pnpm --version` | `npm install -g pnpm` |
| 🤖 Claude Code | `claude --version` | [claude.com/download](https://claude.com/download) |
| 🌿 Git | `git --version` | Mac 自带 · Win: [git-scm.com](https://git-scm.com) |

> 💡 **不需要装 Docker、不需要装数据库**。生成的项目默认用 SQLite 文件数据库，零配置。只有正式上线换 PostgreSQL 时才需要 Docker（见使用手册 § 4.4）。

## 2. 安装（就这两步）

```bash
git clone https://github.com/your-org/youzi-init-project.git ~/youzi-init-project
cd ~/youzi-init-project && ./install.sh install
```

装完 `~/.claude/skills/` 里会多出 3 个技能：`yz-init-admin` / `yz-init-server` / `yz-init-ui`。

> ⚠️ **完全退出 Claude Code 再重开**（Mac: Cmd+Q / Win: Alt+F4），否则识别不到新技能。

## 3. 验证

```bash
./install.sh status
```

3 个技能都是 ✅ 就装好了。也可以打开 Claude Code 输入 `/yz-init`，出现自动补全 = 成功。

## 4. 更新

```bash
cd ~/youzi-init-project && git pull
```

重启 Claude Code 生效。已生成的项目不受影响。

## 5. 卸载

```bash
cd ~/youzi-init-project && ./install.sh uninstall   # 删技能
rm -rf ~/youzi-init-project                         # 删源码（可选）
```

## 6. 常见问题

| 问题 | 解决 |
|---|---|
| `python: command not found` | 用 `python3`（Mac 自带）；仍没有就按上表装 |
| 提示缺 jinja2 | `pip3 install jinja2` |
| `pnpm: command not found` | `npm install -g pnpm` |
| 装完不识别 `/yz-init-admin` | 完全退出 Claude Code 再重开；还不行重跑 `./install.sh install` |
| 提示「目标已存在」 | 之前装过，按提示输 `y` 覆盖即可 |
| 报「缺少 templates/ 目录」 | 确认在仓库根目录执行，或重新 clone |

---

**下一步**：打开 Claude Code 输入 `/yz-init-admin my-app`，跟着 [📖 使用手册.md](使用手册.md) 走。
