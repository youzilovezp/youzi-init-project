# 📦 安装说明

> 只讲安装 / 更新 / 卸载，全程约 3 分钟。
> 怎么生成项目，看 [📖 使用手册.md](使用手册.md)。

---

## 这个工具能装到哪些 AI 工具里

四个都支持，装几个由你决定：

| AI 工具 | 官网 |
|---|---|
| Claude Code | [claude.com/download](https://claude.com/download) |
| opencode | [opencode.ai](https://opencode.ai) |
| Codex | [openai.com/codex](https://openai.com/codex) |
| EasyCode | [easycode](https://easycode.dev) |

装好哪个，安装脚本就会自动发现哪个，**不需要你做任何选择**。

---

## 第 1 步：装好基础工具

先逐行检查下面 5 样。**每条命令都复制到「终端」里按回车**。

### 1.1 检查 Python

```bash
python3 --version
```

- 显示 `Python 3.11` 或更高 → ✅ 通过
- 报错或版本低 → 去 [python.org](https://python.org) 下载安装

### 1.2 检查 Node.js

```bash
node --version
```

- 显示 `v18` 或更高 → ✅ 通过
- 报错 → 去 [nodejs.org](https://nodejs.org) 下载 LTS 版

### 1.3 检查 pnpm

```bash
pnpm --version
```

- 显示版本号 → ✅ 通过
- 报错 → 运行：`npm install -g pnpm`

### 1.4 检查 Git

```bash
git --version
```

- 显示版本号 → ✅ 通过
- 报错 → Mac 自带不用装；Linux 运行 `sudo apt install git`

### 1.5 检查 Docker（可选）

```bash
docker --version
```

- 显示版本号 → ✅ 通过
- 报错 → 两个选择：
  - 去 [docker.com](https://docker.com) 安装
  - 或者不装——之后把项目数据库切到 SQLite 也行（见使用手册）

> 💡 只要 AI 工具（Claude Code / opencode / Codex / EasyCode）里**任意装好了一个**，就可以继续下一步。

---

## 第 2 步：下载本工具

```bash
git clone https://github.com/your-org/youzi-init-project.git ~/youzi-init-project
```

---

## 第 3 步：进入目录

```bash
cd ~/youzi-init-project
```

---

## 第 4 步：安装

```bash
./install.sh install
```

看到每个技能后面出现 `✅` 就是装好了。

它会自动装到**所有**已检测到的 AI 工具里：

| AI 工具 | 安装位置 |
|---|---|
| Claude Code | `~/.claude/skills/yz-init-*` |
| opencode | `~/.config/opencode/skills/yz-init-*` |
| Codex | `~/.codex/skills/yz-init-*` |
| EasyCode | `~/.easycode/skills/yz-init-*` |

只想装其中一个？加 `--platform`：

```bash
./install.sh install --platform claude    # 只装 Claude Code
./install.sh install --platform opencode  # 只装 opencode
./install.sh install --platform codex     # 只装 Codex
./install.sh install --platform easycode  # 只装 EasyCode
```

---

## 第 5 步：重启 AI 工具

**这一步必须做**，否则 AI 工具识别不到新技能。

- Claude Code：按 `Cmd + Q`（Mac）完全退出，再重新打开
- opencode：关掉终端窗口，重新开一个
- Codex / EasyCode：退出后重新打开

---

## 第 6 步：验证

```bash
cd ~/youzi-init-project && ./install.sh status
```

每个平台 3 个技能都显示 `✅ SKILL.md` 就成功了。

再打开你的 AI 工具，输入 `/yz-init`，跳出自动补全 = 完全成功。

---

## 更新

两条命令：

```bash
cd ~/youzi-init-project
git pull
```

然后按第 5 步重启 AI 工具。已生成的项目不受影响。

---

## 卸载

```bash
cd ~/youzi-init-project
./install.sh uninstall
```

想连源码一起删（可选）：

```bash
rm -rf ~/youzi-init-project
```

---

## 常见问题

| 问题 | 解决 |
|---|---|
| `python: command not found` | 用 `python3`（Mac 自带）；仍没有按第 1.1 节安装 |
| 提示缺 jinja2 | `pip3 install jinja2` |
| `pnpm: command not found` | `npm install -g pnpm` |
| 装完不识别 `/yz-init-admin` | 按第 5 步**完全重启** AI 工具；还不行跑 `./install.sh status` 检查 |
| 提示「目标已存在」 | 之前装过，按提示输 `y` 覆盖 |
| 没检测到我的 AI 工具 | `./install.sh install --platform <平台名>` 强制指定 |
| Windows 用户 | 先装 [WSL](https://learn.microsoft.com/windows/wsl/install)：PowerShell 里运行 `wsl --install`，重启后在 WSL 里从第 1 步开始 |

---

**下一步**：打开你的 AI 工具，输入 `/yz-init-admin my-app`，跟着 [📖 使用手册.md](使用手册.md) 走。
