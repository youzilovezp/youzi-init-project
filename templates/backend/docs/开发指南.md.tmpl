# {{ project_title }} 后端开发指南

## 1. 环境准备

- Python 3.11+
- Docker & Docker Compose（用于启动中间件）

## 2. 安装依赖

```bash
cd backend

# 推荐：使用 uv
uv venv .venv
source .venv/bin/activate
uv pip install -e ".[dev]"

# 或使用 pip
python -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"
```

## 3. 启动中间件

在工程根目录：

```bash
make start
# 或
docker compose up -d
```

启动后包含：postgres + redis + **adminer（数据库 UI）**。

## 4. 启动开发服务器

在工程根目录：

```bash
make backend-dev
```

或直接：

```bash
cd backend && uvicorn app.main:app --reload --host 0.0.0.0 --port {{ backend_port }}
```

启动后日志会打印：

```
🔑 默认管理员密码：<随机 16 位>（生产环境请立即修改！）
✅ demo 用户已创建：manager / alice / bob（密码均为 Demo@123）
```

启动后访问：

- Swagger UI：http://localhost:{{ backend_port }}/docs
- ReDoc：http://localhost:{{ backend_port }}/redoc
- 健康检查：http://localhost:{{ backend_port }}/health
- 数据库 UI：http://localhost:8080

## 5. VSCode 调试

打开 `backend/.vscode/launch.json` 已包含三种配置：

- **FastAPI: Debug Server** —— 按 F5 一键启动并断点调试
- **FastAPI: Current Test File** —— 调试当前打开的测试
- **Pytest: All Tests** —— 调试并跑全部测试

> 需要先安装 `ms-python.debugpy` 扩展（启动时 VSCode 会提示）。

## 6. 数据库（自动维护）

后端启动时会**自动维护表结构**，无需手动跑 alembic。具体行为：

| 状态 | 行为 |
|---|---|
| 首次启动（数据库无 `alembic_version` 表）| 自动用 `metadata.create_all` 建表 + `alembic stamp head` 打 baseline + 插入种子数据 |
| 后续启动（已有 `alembic_version` 表）| 自动执行 `alembic upgrade head` + 插入种子数据（仅新增项） |
| 任何步骤失败 | 记录警告但**不阻塞应用启动** |

> ⚠️ 如果 alembic 失败，启动日志会给出**中文诊断**（连接失败/数据库不存在/版本冲突），按提示修复即可。

## 7. 添加新业务模块

**强烈推荐**用一键脚本——比手写 5 个文件快 10 倍：

```bash
python scripts/add_module.py order --title "订单管理"
```

脚本自动生成：

| 文件 | 内容 |
|---|---|
| `app/models/order.py` | SQLAlchemy 模型 |
| `app/schemas/order.py` | Pydantic schema |
| `app/crud/order.py` | CRUD 类（继承 CRUDBase） |
| `app/api/v1/endpoints/order.py` | FastAPI 路由 |
| `src/api/order.ts` | 前端 axios 接口 |
| `src/views/order/index.vue` | 前端 CRUD 页面 |

**你需要做的 4 步**（脚本会再次提示）：

1. `backend/app/models/__init__.py` 导入新模型
2. `backend/app/api/v1/router.py` 注册路由
3. `frontend/src/router/index.ts` 添加路由
4. `frontend/src/layouts/BasicLayout.vue` 添加菜单

跑完上述 4 步后，启动后端即可看到新模块。

## 8. 默认账号

| 账号 | 密码 | 角色 |
|---|---|---|
| `admin` | 启动时控制台打印（16 位随机） | 超级管理员（is_superuser=true） |
| `manager` | `Demo@123` | 部门经理（demo 数据） |
| `alice` | `Demo@123` | 普通员工（demo 数据） |
| `bob` | `Demo@123` | 普通员工（demo 数据） |

> ⚠️ 生产环境必须修改 admin 密码。用 `make reset-admin` 一键随机重置，或 `make admin-pass NEW=YourNewPass!2025` 指定。

## 9. 测试

```bash
cd backend
pytest                 # 全部测试
pytest -v              # 详细输出
pytest --cov=app       # 覆盖率
```

## 10. 常见问题

### Q: 启动报错 `SECRET_KEY 不能为空`

A: `.env` 文件不存在或 `SECRET_KEY` 被注释掉。`make backend-dev` 会自动从 `.env.example` 复制。

### Q: 数据库连接失败

A: 看启动日志的中文诊断。常见原因：
- 中间件没启动 → `make start`
- 端口被占用 → `make stop` 后 `lsof -i :5432` 查
- 密码不一致 → 重新跑 `make reset-admin` 不影响，DB 密码在 .env

### Q: 改了模型没生效

A: alembic 自动检测版本号自动迁移，但新增字段可能需要手动处理：
- 增加列：自动迁移（默认值）
- 改类型/删字段：需手动 `make db-reset` ⚠️（清数据）

### Q: 忘了 admin 密码

A: `make reset-admin` —— 会打印新随机密码。

### Q: 想重置整个数据库

A: `make db-reset` —— 删除所有数据卷后重建。

### Q: 端口 8000 被占用

A: 修改 `.env` 中的 `PORT`，或关闭占用进程：`lsof -i :8000`。

### Q: 想看表里到底有什么

A: 打开 http://localhost:8080（adminer），登录看数据。
