# {{ project_title }} 后端配置说明

> 所有配置项都在 `app/core/config.py` 中定义，通过 pydantic-settings 从 `.env` 文件加载。

## 1. 配置文件加载优先级

1. 环境变量（最高）
2. `.env` 文件
3. `Settings` 类中的默认值（最低）

> **注意**：业务代码中只能通过 `from app.core.config import settings` 访问，禁止直接 `os.getenv()`。

## 2. 配置项详解

### 应用基础

| 变量 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `APP_NAME` | str | `{{ project_title }}` | 应用名，会出现在 OpenAPI 标题 |
| `APP_ENV` | enum | `dev` | `dev` / `test` / `prod` |
| `DEBUG` | bool | `true` | 是否启用调试模式 |
| `API_V1_PREFIX` | str | `/api/v1` | API 路由前缀 |

### 安全

| 变量 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `SECRET_KEY` | str | **必填** | JWT 签名密钥，生产环境用 `openssl rand -hex 32` 生成 |
| `JWT_ALGORITHM` | str | `HS256` | JWT 算法 |
| `JWT_EXPIRE_MINUTES` | int | `1440` | Token 过期时间（分钟）|
| `ACCESS_TOKEN_EXPIRE_SECONDS` | int | `86400` | 同上（秒），用于返回前端 |

### 服务监听

| 变量 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `HOST` | str | `0.0.0.0` | 监听地址 |
| `PORT` | int | `8000` | 监听端口 |
| `WORKERS` | int | `1` | Uvicorn worker 数 |

### 数据库

| 变量 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `DB_DRIVER` | str | `{{ db_driver }}` | 异步驱动（asyncpg / aiomysql）|
| `POSTGRES_*` / `MYSQL_*` | - | - | 数据库连接参数 |
| `DB_ECHO` | bool | `false` | 是否打印 SQL 日志 |
| `DB_POOL_SIZE` | int | `10` | 连接池大小 |
| `DB_MAX_OVERFLOW` | int | `20` | 超出池大小后允许的最大连接数 |

> **多环境切换**：通过修改 `DB_DRIVER` 与对应的连接参数即可。`DATABASE_URL` 属性会根据 `DB_DRIVER` 自动拼接。

### Redis {{ '✅' if enable_redis else '（未启用）' }}

| 变量 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `REDIS_HOST` | str | `localhost` | |
| `REDIS_PORT` | int | `6379` | |
| `REDIS_DB` | int | `0` | |
| `REDIS_PASSWORD` | str | - | 无密码则留空 |

### RabbitMQ {{ '✅' if enable_rabbitmq else '（未启用）' }}

| 变量 | 类型 | 默认值 |
|---|---|---|
| `RABBITMQ_HOST` | str | `localhost` |
| `RABBITMQ_PORT` | int | `5672` |
| `RABBITMQ_USER` | str | `{{ project_name }}` |
| `RABBITMQ_PASSWORD` | str | `{{ project_name }}` |
| `RABBITMQ_VHOST` | str | `/` |

### Celery {{ '✅' if enable_celery else '（未启用）' }}

| 变量 | 类型 | 说明 |
|---|---|---|
| `CELERY_BROKER_URL` | str | 留空则使用 `REDIS_URL` |
| `CELERY_RESULT_BACKEND` | str | 同上 |

### MinIO {{ '✅' if enable_minio else '（未启用）' }}

| 变量 | 类型 | 默认值 |
|---|---|---|
| `MINIO_ENDPOINT` | str | `localhost:9000` |
| `MINIO_ACCESS_KEY` | str | `minioadmin` |
| `MINIO_SECRET_KEY` | str | `minioadmin` |
| `MINIO_BUCKET` | str | `{{ project_name }}` |
| `MINIO_SECURE` | bool | `false` |

### CORS

```env
CORS_ORIGINS=["http://localhost:199310","http://localhost:3000"]
```

JSON 数组格式，按需添加域名。生产环境只保留可信域名。

### 日志

| 变量 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `LOG_LEVEL` | str | `INFO` | DEBUG/INFO/WARNING/ERROR |
| `LOG_FORMAT` | enum | `text` | `text` / `json`（生产推荐 json）|

### 初始化

| 变量 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `INITIAL_ADMIN_USERNAME` | str | `admin` | 首次启动时创建的管理员用户名 |
| `INITIAL_ADMIN_PASSWORD` | str | `youzi@123456` | 默认密码，**生产环境必须改** |
| `INITIAL_ADMIN_EMAIL` | str | `admin@example.com` | 邮箱 |

### 数据库自动维护

| 变量 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `AUTO_CREATE_TABLES` | bool | `true` | 首次启动（无 alembic_version 表）时自动用 metadata.create_all 建表 |
| `AUTO_MIGRATE` | bool | `true` | 后续启动自动执行 `alembic upgrade head` |
| `AUTO_SEED_DATA` | bool | `true` | 自动插入种子数据（默认管理员、默认角色）|

> **生产环境建议**：将 `AUTO_MIGRATE` 保持开启（推荐）；将 `AUTO_CREATE_TABLES` 设为 `false` 以防生产数据被覆盖。

## 3. 多环境实践

```
backend/
├── .env             # 当前环境（gitignore）
├── .env.example     # 样例（提交）
├── .env.dev         # 开发环境（提交）
├── .env.prod        # 生产环境（提交）
└── .env.test        # 测试环境（提交）
```

加载顺序：pydantic-settings 会按 `env_file` 加载 `.env`，如需按环境加载：

```python
class Settings(BaseSettings):
    model_config = SettingsConfigDict(
        env_file=f".env.{os.getenv('APP_ENV', 'dev')}",
        ...
    )
```

## 4. 安全建议

1. **永远不要把 `.env` 提交到 git**。
2. **生产环境必须使用强随机 SECRET_KEY**。
3. **生产环境关闭 DEBUG、DB_ECHO**。
4. **生产环境 LOG_FORMAT 设为 json**，便于日志聚合。
5. **CORS 不要写 `*`**，只列出可信域名。
