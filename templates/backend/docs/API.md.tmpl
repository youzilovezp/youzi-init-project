# {{ project_title }} 后端 API 文档

## 1. 接口总览

| 模块 | 路径前缀 | 说明 |
|---|---|---|
| 健康检查 | `/health` | 不需要鉴权 |
| 认证 | `/api/v1/auth` | 登录、登出、当前用户 |
| 用户管理 | `/api/v1/users` | CRUD、修改密码 |
| 角色管理 | `/api/v1/roles` | CRUD |

## 2. 统一响应格式

```json
{
  "code": 0,
  "message": "ok",
  "data": { ... }
}
```

## 3. 状态码

| 业务码 | 含义 |
|---|---|
| 0 | 成功 |
| 40001 | 用户名已存在 |
| 40100 | 认证失败 |
| 40300 | 权限不足 |
| 40400 | 资源不存在 |
| 42200 | 参数校验失败 |
| 50000 | 服务器内部错误 |

## 4. 认证

所有需要鉴权的接口必须携带：

```
Authorization: Bearer <access_token>
```

## 5. 接口示例

### 登录

```http
POST /api/v1/auth/login
Content-Type: application/json

{
  "username": "admin",
  "password": "youzi@123456"
}
```

响应：

```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "access_token": "eyJhbGc...",
    "token_type": "bearer",
    "expires_in": 86400,
    "user": { ... }
  }
}
```

### 当前用户

```http
GET /api/v1/auth/me
Authorization: Bearer <token>
```

### 用户列表（分页）

```http
GET /api/v1/users?page=1&page_size=20&username=admin
Authorization: Bearer <token>
```

### 创建用户

```http
POST /api/v1/users
Authorization: Bearer <token>
Content-Type: application/json

{
  "username": "zhangsan",
  "email": "zhangsan@example.com",
  "password": "P@ssw0rd!",
  "nickname": "张三",
  "role_id": 1
}
```

## 6. Swagger / Postman

- Swagger UI：http://localhost:199311/docs
- ReDoc：http://localhost:199311/redoc
- OpenAPI JSON：http://localhost:199311/api/v1/openapi.json

在 Swagger UI 中可直接点击 "Authorize" 输入 token 进行调试。导出 OpenAPI JSON 后可在 Postman 中通过 `Import → Import as API` 导入。
