# {{ project_title }} 后端

基于 FastAPI + SQLAlchemy 2.0（异步）的管理系统后端。

## 启动

```bash
# 启动中间件（在工程根目录）
cd .. && make start

# 安装依赖 + 启动
uv pip install -e ".[dev]" || pip install -e ".[dev]"
uvicorn app.main:app --reload
```

或者用工程根目录的 Makefile：

```bash
cd .. && make backend-dev
```

启动后访问：

- API 文档：http://localhost:{{ backend_port }}/docs
- 数据库 UI：http://localhost:8080（adminer）

## 启动后日志会打印

```
🔑 默认管理员密码：xxx（生产环境请立即修改！）
✅ demo 用户已创建：manager / alice / bob（密码均为 Demo@123）
```

## 项目结构

```
backend/
├── app/                # 应用代码
│   ├── api/            # 路由层
│   ├── core/           # 配置、安全、异常、日志
│   ├── crud/           # 数据访问
│   ├── db/             # 数据库连接（含 init_db.py 自动建表）
│   ├── middleware/     # 中间件
│   ├── models/         # ORM 模型
│   ├── schemas/        # Pydantic 模型
│   ├── services/       # 业务逻辑
│   ├── deps.py         # 依赖注入
│   └── main.py         # 入口
├── alembic/            # 数据库迁移
├── docs/               # 架构/技术栈/配置/开发/API 文档
├── tests/              # 测试
├── scripts/            # 辅助脚本
│   ├── reset_admin.py   # 重置管理员密码
│   └── start.sh        # 启动脚本（从 .env 读 PORT）
├── docker/             # Dockerfile
├── .env.example        # 环境变量样例
└── pyproject.toml
```

## 加新业务模块

```bash
python scripts/add_module.py order --title "订单管理"
```

脚本自动生成 6 个文件（model/schema/crud/router/view/api），并打印你需要手动注册的 4 处地方。

## 日常用到的命令

```bash
make help               # 查看完整命令列表
make backend-dev        # 启动后端（自动建表 + 种子）
make db-shell           # 进入 psql
make reset-admin        # 重置管理员密码
make admin-pass NEW=xxx # 重置为指定密码
make backup             # 备份数据库
make restore FILE=...   # 从备份恢复
make db-reset           # ⚠️ 重置数据库
```

## 文档索引

- [架构说明](docs/架构说明.md)
- [配置说明](docs/配置说明.md)
- [开发指南](docs/开发指南.md)
- [API 文档](docs/API文档.md)

## License

MIT
