# {{ project_title }} 前端架构说明

## 1. 整体架构

```
┌──────────────────────────────────────────────────┐
│                      App.vue                       │
└─────────────────────┬────────────────────────────┘
                      │
                      ▼
┌──────────────────────────────────────────────────┐
│                  Vue Router                        │
│   - 路由配置 + 全局守卫（鉴权/角色/标题）           │
└─────────────────────┬────────────────────────────┘
                      │
        ┌─────────────┴─────────────┐
        ▼                           ▼
┌──────────────────┐       ┌────────────────────┐
│  BasicLayout     │       │   公开页面          │
│  - 侧边栏菜单    │       │  /login /register   │
│  - 顶栏          │       └────────────────────┘
│  - <router-view/>│
└────────┬─────────┘
         │
         ▼
┌──────────────────────────────────────────────────┐
│                  Views（页面）                     │
│   dashboard / system/user / system/role           │
└─────────────────────┬────────────────────────────┘
                      │
                      ▼
┌──────────────────────────────────────────────────┐
│          Stores（Pinia 状态管理）                    │
│   userStore / appStore                            │
└─────────────────────┬────────────────────────────┘
                      │
                      ▼
┌──────────────────────────────────────────────────┐
│               API 层（src/api）                    │
│   - 业务模块：user.ts / auth.ts / role.ts          │
│   - 基础：request.ts（Axios 实例 + 拦截器）         │
│   - 类型：types.ts（统一响应/分页/UserInfo）       │
└─────────────────────┬────────────────────────────┘
                      │
                      ▼
                  ┌─────────┐
                  │ 后端 API │
                  └─────────┘
```

## 2. 目录说明

```
src/
├── api/            # 接口层：按业务模块拆分；统一封装在 request.ts
├── assets/         # 静态资源（图片、字体）
├── components/     # 公共组件
├── config/         # 应用配置常量
├── layouts/        # 布局组件（BasicLayout 等）
├── locales/        # 国际化文案
├── router/         # 路由 + 全局守卫
├── stores/         # Pinia store
├── styles/         # 全局样式（SCSS）
├── types/          # TypeScript 类型声明
├── utils/          # 工具函数
├── views/          # 页面（按业务模块组织）
│   ├── dashboard/
│   ├── login/
│   └── system/
│       ├── user/
│       └── role/
├── App.vue         # 根组件
└── main.ts         # 入口
```

## 3. 数据流

```
用户操作
   │
   ▼
Vue 组件（dispatch）
   │
   ▼
Pinia Store（业务编排）
   │
   ▼
API 模块（types/接口定义）
   │
   ▼
request.ts（Axios 拦截器：token / 错误处理）
   │
   ▼
后端 API
```

## 4. 鉴权流程

1. 用户在 `/login` 输入用户名密码
2. 调用 `authApi.login()` 拿到 `access_token`
3. 写入 Pinia + localStorage
4. `router.beforeEach` 守卫检查 token，未登录跳 `/login`
5. 已登录但无 `userInfo` 时调 `getMe()` 拉取用户信息
6. 受保护路由检查 `meta.requiresAdmin`

## 5. 代码与配置隔离

| 维度 | 做法 |
|---|---|
| 环境变量 | `.env.development` / `.env.production` 中定义 `VITE_*` 变量 |
| 编译期 | 通过 `import.meta.env.VITE_XXX` 访问，构建时静态替换 |
| 业务代码 | 不允许直接读 `process.env`，统一通过 `src/config/index.ts` 间接访问 |
| 多环境 | Vite 通过 `--mode` 切换，自动加载对应 `.env.[mode]` |

## 6. 公共约定

1. **命名**：组件大驼峰（PascalCase），方法/变量小驼峰，常量全大写下划线。
2. **TypeScript**：所有组件、API 必须有类型，禁止 `any`。
3. **样式**：SCSS + scoped，避免全局污染。
4. **图标**：使用 Element Plus Icons，按需注册。
5. **请求**：所有 HTTP 调用必须走 `request`，禁止直接 `axios`。
