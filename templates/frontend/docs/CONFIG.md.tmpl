# {{ project_title }} 前端配置说明

## 1. 环境变量文件

| 文件 | 用途 | 是否提交 |
|---|---|---|
| `.env.development` | 开发环境 | ✅ |
| `.env.production` | 生产环境 | ✅ |
| `.env.local` | 本地覆盖（个人配置）| ❌ gitignore |
| `.env.*.local` | 特定环境覆盖 | ❌ gitignore |

## 2. 变量命名规范

- **必须**以 `VITE_` 前缀，否则 Vite 不会暴露给客户端代码。
- **不要**在变量里放密钥、token。

## 3. 变量说明

| 变量 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `VITE_APP_TITLE` | str | - | 网站标题 |
| `VITE_API_BASE_URL` | str | `/api/v1` | API 基础路径 |
| `VITE_PROXY_TARGET` | str | `http://localhost:8000` | 开发环境代理目标 |
| `VITE_TOKEN_KEY` | str | `access_token` | localStorage 中 token 的 key |

## 4. 业务代码中访问

```ts
import.meta.env.VITE_APP_TITLE          // 直接访问
```

更推荐统一通过 `src/config/index.ts`：

```ts
import { APP_TITLE, API_BASE_URL } from '@/config'
```

## 5. 多环境构建

```bash
# 开发环境（默认）
npm run dev

# 生产构建
npm run build

# 自定义环境（如 staging）
vite build --mode staging
# 此时会加载 .env.staging
```

## 6. 代理配置

`vite.config.ts` 中已配置 `/api` 代理到后端：

```ts
server: {
  proxy: {
    '/api': {
      target: 'http://localhost:8000',
      changeOrigin: true,
    },
  },
}
```

开发环境通过 `VITE_PROXY_TARGET` 可修改目标地址。生产环境通过 Nginx 反代。

## 7. 生产部署

构建产物在 `dist/` 目录，推荐使用 Nginx 托管：

```nginx
server {
  listen 80;
  server_name your-domain.com;
  root /var/www/{{ project_name }}/dist;
  index index.html;

  # SPA 路由 fallback
  location / {
    try_files $uri $uri/ /index.html;
  }

  # 反向代理 API
  location /api/ {
    proxy_pass http://backend:8000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
  }
}
```
